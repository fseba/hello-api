version: "2"
linters:
  enable:
    - gosec
    - godot
    - misspell
    - staticcheck
  settings:
    staticcheck:
      checks: ["all", "ST1*"]

output:
  formats:
    text:
      print-issued-lines: false
      print-linter-name: true
      colors: true
